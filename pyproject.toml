[tool.poetry]
name = "fitbit-agent"
version = "0.1.0"
description = "Fitbit data extraction agent"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
